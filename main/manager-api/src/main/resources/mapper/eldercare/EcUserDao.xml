<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="xiaozhi.modules.eldercare.dao.EcUserDao">

    <resultMap type="xiaozhi.modules.eldercare.entity.EcUserEntity" id="ecUserMap">
        <result property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="realName" column="real_name"/>
        <result property="phone" column="phone"/>
        <result property="email" column="email"/>
        <result property="elderName" column="elder_name"/>
        <result property="elderInfo" column="elder_info"/>
        <result property="status" column="status"/>
        <result property="creator" column="creator"/>
        <result property="createDate" column="create_date"/>
        <result property="updater" column="updater"/>
        <result property="updateDate" column="update_date"/>
    </resultMap>

    <select id="getByUsername" resultMap="ecUserMap">
        SELECT * FROM ec_users WHERE username = #{username}
    </select>

    <select id="getByPhone" resultMap="ecUserMap">
        SELECT * FROM ec_users WHERE phone = #{phone}
    </select>

    <select id="getByEmail" resultMap="ecUserMap">
        SELECT * FROM ec_users WHERE email = #{email}
    </select>

</mapper>
